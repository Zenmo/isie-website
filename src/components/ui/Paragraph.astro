---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"p">;

const { class: className, ...rest } = Astro.props;
---

<style>
  p {
    font-weight: lighter;
  }
</style>
<p class:list={["text-muted-foreground text-lg mb-8", className]} {...rest}>
  <slot />
</p>
